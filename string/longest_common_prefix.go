package string

import "fmt"

// Implement a function that finds the longest common prefix in a set of strings.

func longestCommonPrefix() {
	// words := []string{"flower", "flow", "flight"}
	fmt.Println("Analyse it more")
}

// func checkPrefix(words []string) string {
// 	if len(words) == 0 {
// 		return ""
// 	}

// 	prefix := words[0]
// 	for _, word := range words[1:] {
// 		for len(prefix) > 0 {
// 			if len(word) >= len(prefix) && word[:len(prefix)] == prefix {
// 				break
// 			}
// 			prefix = prefix[:len(prefix)-1]
// 		}
// 		if prefix == "" {
// 			return ""
// 		}
// 	}
// 	return prefix
// }
